apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service
  namespace: ecommerce-apps
spec:
  replicas: 1
  selector:
    matchLabels:
      app: order-service
  template:
    metadata:
      labels:
        app: order-service
    spec:
      containers:
      - name: order-service
        image: 644981759771.dkr.ecr.us-east-1.amazonaws.com/prod-order-service:latest
        ports:
        - containerPort: 3004
        env:
        - name: PORT
          value: "3004"
        - name: MONGO_URI
          value: "mongodb://mongodb:27017/shop"
---
apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: ecommerce-apps
spec:
  selector:
    app: order-service
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3004
